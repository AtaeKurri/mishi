<form>
    <div class="form-group">
        <label><b>Contexte</b></label>
        <select name="AMP" style="width: 99.3%;">
            {{#select AMP}}
            {{#each cp as |name type|}}
            <option value="{{type}}">{{name.name}}</option>
            {{/each}}
            {{/select}}
        </select>
    </div>
</form>